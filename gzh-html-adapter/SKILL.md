---
name: gzh-html-adapter
description: "将已有 Markdown、HTML、DOCX、PDF 或纯文本文章转换为微信公众号可粘贴的 HTML。保留原文内容，套用固定的蓝白视觉系统：白底、深蓝小标题加主蓝左竖条、深蓝表头配浅蓝斑马纹表格、蓝系原生 HTML 图表，宽表在手机上转为纵向分组块。文章标题不写进正文，留给公众号后台的标题栏，正文最高标题层级为 h2。输出纯正文片段和带“复制到公众号”按钮的预览页；正文禁止 h1、图片、SVG、Canvas、脚本和外部样式，色值与字号由校验器强制。适用于“转成公众号 HTML”“保留内容只做微信适配”“图表不要图片”“加复制到公众号按钮”等请求。"
---

# 微信公众号 HTML 适配

## 原则与边界

这个 Skill 只转换已有文章。原文的观点、事实、数字、单位、链接和先后顺序必须保留。

允许做的处理：

- 恢复或整理标题层级、段落、列表、引用、表格、脚注和链接。
- 拆开因文件转换而粘连的段落，合并 PDF 中的硬断行。
- 将符合条件的数据表转换为原生 HTML 图表。
- 根据文章题材和结构确定概括性的设计方向，并据此处理层级、留白和分组。
- 按固定的蓝白视觉系统补齐微信公众号需要的内联 CSS。

禁止做的处理：

- **把文章标题写进正文。** 公众号后台有独立的标题栏，标题在那里填。正文最高标题层级是
  `h2`，不得出现 `h1`，也不得用大字号段落把标题再写一遍。
- 补写标题、摘要、结论、过渡段、作者介绍或行动号召。
- 改写原句、调整观点顺序、删去重复内容或突出模型自行判断的重点。
- 自行拟定色值、字号或间距。配色、字号、间距固定于
  [references/visual-system.md](references/visual-system.md)，设计方向只决定结构与节奏。
- 在正文中加入图片、SVG、Canvas、视频、脚本、外部 CSS 或外部字体。

输入含图片时，读取其中的文字、表格和可辨认数据；正文只保留提取结果、原题注、替代文字和来源链接。不得把图片写入输出 HTML。

## 产物与处理流程

每次转换交付两个文件：

1. `{原文件名}_公众号正文.html`
   - 只有一个根 `<section>`。
   - 所有样式写在元素的 `style` 属性内。
   - 不含文档外壳、按钮或脚本。
2. `{原文件名}_公众号预览.html`
   - 在正文之外提供“复制到公众号”按钮。
   - 按钮只复制正文区域的富文本。
   - 预览页的工具栏、按钮、样式和脚本不会进入剪贴板正文。

按以下顺序执行：

1. **读取输入。** Markdown、HTML、DOCX、PDF 和纯文本的处理规则见
   [references/input-normalization.md](references/input-normalization.md)。遇到扫描页或文字图片时使用中文 OCR。
2. **整理结构。** 保留原文内容和顺序，只修复标题、段落、列表、引用、表格、脚注和链接的结构。
   原文开头的文章标题在这一步摘掉，不进正文；把它单独写在交付说明里，供粘贴到公众号后台的标题栏。
3. **判断设计方向。** 按
   [references/design-direction.md](references/design-direction.md)
   根据文章类型、信息密度、数据比例和叙事方式确定方向。方向只决定层级密度、留白节奏、分组方式和是否转图表，不决定颜色。
4. **处理数据表。** 按
   [references/native-charts.md](references/native-charts.md)
   判断是否转为 KPI、数据表、条形图、进度图或时间线。无法从原文确认的值不得进入图表。
5. **生成正文。** 结构规则见
   [references/wechat-html-rules.md](references/wechat-html-rules.md)，配色、字号和间距照抄
   [references/visual-system.md](references/visual-system.md)
   的片段表与 [assets/sample-body.html](assets/sample-body.html) 样张。不得自行拟定色值、字号或间距。
6. **校验正文。**

   ```bash
   python3 <SKILL_ROOT>/scripts/validate_gzh_html.py \
     <原文件名>_公众号正文.html
   ```

   校验结果有错误时修正正文并重新运行，直到退出码为 `0`。
7. **生成预览页。**

   ```bash
   python3 <SKILL_ROOT>/scripts/build_preview.py \
     <原文件名>_公众号正文.html
   ```

8. **交付。** 先写一行 `文章标题：<原文标题>`，说明它要粘到公众号后台的标题栏、不在正文里；
   再写一行 `设计方向：<方向及一句判断理由>`，然后返回两个文件的路径和校验结果。这一行只出现在聊天交付说明中，不写入正文。没有执行微信公众号后台粘贴测试时，只说明结构校验通过，不声称后台粘贴已经人工确认。

## 必须避免的问题

- **把预览页当正文交付。** 公众号正文只能使用干净的 `<section>` 片段；完整文档外壳只用于本地预览和复制。
- **把 CSS 放进 `<style>`。** 微信编辑器会过滤文档级样式。正文元素必须各自带完整的内联样式。
- **漏掉文字包裹。** 可见文字必须放在 `<span leaf="">` 内，否则复制后可能丢失样式。
- **把图片换一种形式继续嵌入。** `img`、SVG、Canvas、CSS `url()`、data URI 和背景图片都属于禁止项。
- **用脚本画图。** 正文不运行 JavaScript。图表必须由普通 HTML 标签、表格结构、百分比宽高和内联 CSS 构成。
- **为了画图补数据。** 图表只能使用原文明确给出的标签、数值、单位和顺序；不插值、不估算、不补齐缺口。
- **看到数字就自动画图。** 只有结构明确的数据表才转换；正文中的零散数字保持正文。
- **把按钮放进复制区域。** “复制到公众号”按钮和状态提示必须在正文节点之外。
- **把文章标题留在正文里。** 常见形式是原文的 `# 一级标题` 被直接转成 `h1` 或大字号加粗段落。
  公众号后台会在正文上方显示标题栏里填的标题，正文再写一遍就是重复。校验器直接拦 `<h1>`。
- **把设计说明写进正文。** 设计方向及理由只用于交付说明，不能成为文章的新段落。
- **自创色值或字号。** 颜色只能取自 `visual-system.md` 的十一个 token，标题字号与间距按该文档的表格取值。
- **带背景色的块漏写 `color`。** 微信 iOS 深色模式会反转背景却不反转继承来的文字，浅蓝底上会出现白字。
- **根背景不是白色。** 根 `<section>` 只能用 `#FFFFFF`，或仅由 `#FFFFFF` 与 `#F2F6FC` 构成的原生渐变。
- **借适配之名改文章。** OCR 修正、断行合并和标题整理不能改变句意、数字或来源。
