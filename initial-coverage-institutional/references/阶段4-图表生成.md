# 阶段 4：图表生成

产物：`[公司]_图表_[日期].zip`，含 40–70 张 PNG（300 DPI）+ 一份图表清单（文件名 + 标题 + 数据来源 + 对应正文章节）。

前置：阶段 1、2、3 产物齐，外部行情数据（股价、可比公司倍数）能获取。缺失则停下补充。

横向比较图同时按 `同业与产业链横向比较.md` 执行。数据只从阶段 3 Excel 的 `O_经营对标`、`V_估值可比`、`T_产业链主题` 读取，必要时回溯 `横向比较_主数据`；不从阶段 1 Markdown 或旧 PNG 手抄。四个 sheets 必须已完成 staging 校验并写清 O/V/T 层级、行情日、期间、币种和来源。

出图时如发现 Excel 缺数或口径有误，回到 staging 校验并更新 Excel 后再出图，不在绘图脚本或图表 `_data` 底稿中直接补写新的业务数字。

中文深度研报图表密度极高（标杆 44–87 张），图和表统一连续编号。这一阶段把前三阶段的数据可视化，阶段 5 全部嵌入正文。

## 图表数据：只画真值，缺数不补造

图表数据点和正文数字一样，受同一套取数纪律约束——不存在"图表是次要数据可以放水"。这是上一次出事最集中的地方（编造指数线、给基石/解禁/融资图凑假明细、股价图插值补点），这一阶段最该守住。

**每张图建 `_data` 底稿。** 每张图配一份数据底稿，列清每个数据系列的每个点来自哪——`系列 | 数值 | 口径 | 时间点 | 来源`。**底稿里指不到来源的系列，不画**。一张图只有总量真值、没有明细真值，就只画总量，别把它拆成编造的明细图。

**禁止合成、插值、示意填充。** 缺数就留空、标「未获取」，或换一种只用真值的画法，绝不 `np.interp` 补点、`np.random` 加噪声、或贴个"示意/近似"就编一条曲线。"示意/近似/约"只能标真实数据的精度，不能给不存在的数据当免责声明——一个标了"示意"的编造数字，和没标的，对读者危害一样。

**必备图缺数就降级或省略。** 某张必备图的数据不够，降级成只画已知值，或省略并注明"数据未获取"，不补造。少一张真图，好过多一张假图。

**出图后必跑反编造 lint。** 全部图生成后，跑 `scripts/anti_fabrication_lint.py`，把图表构建脚本和各 `_data` 底稿传进去，命中逐条清掉（指到真实来源或删系列）才进阶段 5。脚本只兜机械可检的痕迹（插值、噪声、示意标签、无源系列），看起来合理的错数还得靠异源验证。

## 技术栈

Python + matplotlib + pandas。两件事必须先解决：

**中文字体**（不配置会导致中文显示为方框乱码）。按平台选择可用的中文字体：

```python
import matplotlib
import matplotlib.pyplot as plt
# macOS 优先级：Songti SC / PingFang SC / Arial Unicode MS
# 也可指定思源宋体/黑体的 ttf 路径
matplotlib.rcParams['font.sans-serif'] = ['Songti SC', 'PingFang SC', 'Arial Unicode MS', 'Heiti SC']
matplotlib.rcParams['axes.unicode_minus'] = False  # 负号正常显示
```

生成第一张图后**务必查看一次**，确认中文不是方框，再批量生成。

**研报配色**。图表用独立的四色板，不与 Word 文字配色共用色值：

```python
PALETTE = ['#1F3A6E',   # 深藏青：主数据系列（柱状、目标公司）
           '#E15759',   # 珊瑚红：次数据系列（折线）/ 强调 / 负值
           '#76A9E8',   # 中蓝：第三系列（堆叠第二层、并列第二组柱）
           '#9AA5B1']   # 灰：基准线、辅助线、参考横线
```

四条约束：

1. **柱用深藏青，线用珊瑚红。** 柱线组合图的两个系列必须一冷一暖——同色系（亮蓝柱 + 深蓝线、亮蓝柱 + 黑线）在双图并列的小尺寸和黑白打印下会糊成一片，这是踩过两次的坑。
2. **珊瑚红同时是次系列、强调色和负值色，但同一张图里只能承担其中一个角色。** 实际不冲突：柱线组合图里它是折线，纯柱图里它是负值柱或高亮柱，两类图不重叠。一张图里既有红线又有红柱，读者就分不出红代表什么了。
3. **堆叠柱和并列柱用深藏青 + 中蓝**（同冷色系但明度差足够），不动珊瑚红——那两种图里珊瑚红要留给负值和高亮。
4. 四色不够用时（多分部堆叠、五家以上可比公司），用深藏青的明度阶梯（`#1F3A6E` → `#76A9E8` → `#B9CDE8`）分层，不引第五个色相。

旧色板一律不得再出现：红系 `#C0202E`、`#1F4E79`、亮蓝 `#2B6EF2` 作柱、黑 `#1A1A1A` 作线。亮蓝 `#2B6EF2` 和深蓝 `#1B3A6B` 继续用于 Word 文字与表头，不用于图表数据系列。

## 图表规范

每张图：

- **标题**只写「[描述] [单位]」，用读者语言，不出现 O/V/T 池等建池代号（写「可比公司」「产业链公司」）。图内不预写最终“图表 N”。阶段 5 在原生 Word 表格和图片全部排定后统一连续编号
- **来源**小字脚注「来源：公司公告、WIND、XX研究所」
- 300 DPI（`dpi=300`）。**尺寸按最终占位反推，图在 Word 里要占满宽度、不许缩小后居中**：全宽图 `figsize` 宽 **7.33 in**（18.6 cm @300dpi = 2197 px），双图并列的窄版宽 **3.66 in**（9.3 cm = 1098 px）
- 文件名使用稳定资产 ID + 描述，如 `peer_v_growth_valuation.png`、`chart_segment_revenue.png`；图表清单另列建议插入顺序。不要把阶段 4 的临时顺序当最终编号
- 数据标签清晰，不重叠、不超出边界、不裁切；坐标轴有单位

### 坐标轴与边框

直角坐标图（柱状图、折线图、面积图、散点图）统一去掉上、右边框线，保留 X / Y 实线；所有图一律不画网格线：

```python
ax.spines['top'].set_visible(False)
ax.spines['right'].set_visible(False)
ax.spines['left'].set_visible(True)     # X / Y 实线保留
ax.spines['bottom'].set_visible(True)
ax.grid(False)                          # 全部图去网格线
```

上边线和右边线永远不要显示。饼图、雷达图、热力图、足球场图、瀑布图没有常规直角坐标轴，按各自惯例处理，不强套这条。

### 双 Y 轴（股价走势对比图）

个股股价和指数点位量纲差几十倍，放同一条 Y 轴个股线会被压平。用 `twinx()` 双轴，两条线都画绝对值、**都用日频逐日收盘**：

```python
import pandas as pd
# 个股、指数都从结算日 K 原始序列读（已落 _data 底稿），不插值、不重采样补点
stock = pd.read_csv('[公司]_daily_close.csv')   # 逐日收盘，结算口径
index = pd.read_csv('[指数]_daily_close.csv')   # 同区间逐日收盘
fig, ax = plt.subplots()
ax.plot(stock.date, stock.close, color=PALETTE[0], label='个股股价（元）')   # 左轴：个股绝对股价
ax2 = ax.twinx()
ax2.plot(index.date, index.close, color=PALETTE[3], label='指数（点）')      # 右轴：指数绝对点位
ax.set_ylabel('股价（元）'); ax2.set_ylabel('指数（点）')
# 左右轴各自图例，或合并图例；ax2 同样去掉 top spine
ax2.spines['top'].set_visible(False)
```

两条线都画绝对值，不要归一化到 100。**数据必须是日频逐日真实收盘**——个股、指数、可比公司价格一律取日频结算序列，落 `_data` 底稿，按真实日频点画线。不要周频重采样、更不要插值补点（上次正是靠插值凑周度平滑曲线出的事）。

### 右栏窄版相对表现图（首页必备）

首页右栏那张相对表现图是独立资产，不是全宽股价图缩小版。它要在 2.8 cm 宽的栏里读得清，必须单独生成：

- 输出宽度按 4.7 cm @ 300 DPI（`figsize=(1.85, 1.33)`，约 555×399 px），Word 里**按 4.7 cm 摆放占满右栏**，不要缩到 3 cm 以下——缩小后刻度和图例看不清，且与右栏其他元素左右边界对不齐
- 个股 vs 对标指数，近 12 个月，**日频逐日真实收盘**，不插值、不重采样
- 对标指数按上市地选：A 股沪深300、港股恒生指数、美股纳斯达克综合指数（科技股）或标普500
- 双 Y 轴绝对值（左轴个股价、右轴指数点位），个股用深藏青 `#1F3A6E`、指数用珊瑚红 `#E15759`
- 窄图里字要能看清：刻度字号 5–6pt，X 轴只保留 4–5 个月份刻度，Y 轴刻度各 3 个，图例放图内左上角、6pt，去掉轴标签文字（栏太窄放不下，口径写在图下小字）
- 无网格线、无上/右边线，与其他图一致

正文里的全宽股价走势对比图仍然保留，两张图数据同源、口径一致。

### 双图并列尺寸

成对/可对照的图（分部增速对比、可比公司倍数、三情景、毛利率 vs 净利率等）在阶段 5 会一行两图并列，**每张占满半栏 9.3 cm**。这类图按窄版生成：`figsize=(3.66, 2.7)` 左右、长宽比偏 4:3、字号适当放大，让它在 9.3 cm 下仍然清楚。单张图按全宽版 `figsize=(7.33, …)` 生成。

**两种尺寸都必须在 Word 里占满各自的位置**——生成时就按最终宽度定 `figsize`，不要生成一个尺寸再靠 Word 缩放去适配，那样要么模糊要么缩成孤立的小图。阶段 5 用无边框二列表格放置（见 `Word文档格式标准.md`）。

## 图表清单（按数据来源映射）

40–70 张，覆盖前三阶段 + 外部数据。下面是骨架，按公司和行业增减。

**必备图（不可缺）**：

- 相对表现图（右栏窄版，首页右栏第 7 个元素）：规格见上方「右栏窄版相对表现图」，缺这张首页排不出来
- 股价走势对比（个股 vs 指数，近 12 个月，正文用全宽版）：**双 Y 轴**——左轴个股绝对股价、右轴指数绝对点位，两者都是绝对值，**严禁指数化 / 归一化到 100**；**日频逐日真实收盘**（约 250 个交易日点），不插值、不重采样
- 分部营业收入（堆叠柱或堆叠面积，历史 + 预测）
- 盈利预测（收入/归母净利/增速，历史 + 5 年预测）
- 估值：SOTP 拆解图（多业务）或可比公司倍数对比
- 经营对标图（市占率 / 毛利率 / 关键经营指标，按行业选）
- 可比公司增长—估值矩阵（盈利公司用利润/EPS 增速 × PE FY1；未盈利/利润失真公司另做收入增速 × PS FY1）
- 产业链定价光谱（按产业链环节比较市值、增长、利润率和 PE/PS）
- 估值足球场图（各方法估值区间 + 当前价 + 目标价）
- DCF 敏感性热力图（若做了 DCF）

**来自阶段 1（公司研究）**：

- 历史沿革时间轴
- 股权结构图
- 组织架构图
- 分部收入占比（饼图或环形）
- 分部毛利率对比
- 费用率与净利率趋势
- 行业市场规模（历史 + 预测 CAGR）
- 产业链图
- 市占率对比
- 竞品经营对标（优先对比柱或点图；雷达图只在量纲归一规则清楚时使用）
- 产业链位置与核心概念股对比
- 管理层/核心团队（可做表）

**来自阶段 2（盈利预测模型）**：

- 分部营收预测（堆叠）
- 分部增速对比
- 毛利率/净利率预测趋势
- 三情景对比（乐观/中性/悲观的收入或利润路径）
- 历史回测对照（模型回算 vs 实际）
- 资本开支/研发投入趋势
- 现金流趋势

**来自阶段 3（估值）**：

- 可比公司倍数散点或对比柱
- V 池可比公司分位统计
- V 池增长—估值气泡图（气泡大小为市值）
- SOTP 分部估值瀑布/堆叠
- DCF 现金流折现瀑布
- DCF 敏感性热力图
- 历史估值通道（PE/PB 历史分位）
- 足球场图

**外部数据**：

- 股价走势对比
- 历史估值通道（需历史股价 + 历史 EPS/BPS）

## 自检

- 40–70 张，必备图全
- 中文不乱码（生成后已核验）
- 配色全报告统一
- 直角坐标图无上 / 右边线、无网格线，X / Y 实线在
- 股价走势对比图双 Y 轴绝对价格、日频逐日真实收盘，未做指数化 / 归一化、未插值
- 成对/可对照图已按窄版（50% 栏宽）生成
- O/V/T 横向图都能回溯到阶段 3 Excel 的同一已验证快照；标题或脚注写清行情日、财务期间、币种和 `N/M` 处理
- V 池 PE 图与 PS 图分开生成，没有把盈利与未盈利公司的两套口径混在同一坐标系
- 每张 300 DPI、有标题和来源脚注
- 文件名用稳定资产 ID，图内未预写最终编号；清单写明建议顺序、源工作表/表名/字段和对应正文章节
- 每张图配 `_data` 底稿，每个系列指得到来源；没有无源 / 示意 / 凑数系列
- 必备图缺数的，已降级或省略并注明，没补造
- 跑 `scripts/anti_fabrication_lint.py` 通过（命中已逐条清除）
- 打包 zip + 清单
